// Object => BOOL
// function isEmptyObj(){}


function isEmptyObj(obj) {
    let isEmpty = true;

    for (let key in a) {
        isEmpty = false;
    }
    return isEmpty;
}

const a ={
    name: 'cc',
    age: 15,
};